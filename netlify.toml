[build]
  publish = "."
  command = ""

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"

# Stufe 2: Aufrufe an /api/* landen bei den Serverfunktionen.
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
